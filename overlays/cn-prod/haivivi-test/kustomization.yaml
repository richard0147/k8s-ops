apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: haivivi-test
#commonLabels:
#  cluster: prod-asia
#  env: prod

resources:                 # ← 决定“发哪些”
  - ../../../base/haivivi-test/test-project
  - ../../../base/haivivi-test/test-ooo
  - ../../../base/haivivi-test/test-aaa

#components:                # ← 插上“公共补丁”
#  - ../../../components/cn-stage/

patches:                   # ← 本组本环境特殊补丁
  - path: patches/syw-test-replica.yaml