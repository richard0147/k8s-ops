apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-web
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: admin-web
          envFrom:
            - secretRef:
                name: stage-env
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 400m
              memory: 512Mi
