apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: haivivi-test
#commonLabels:
#  cluster: prod-asia
#  env: prod
namePrefix: cn-stage-haivivi-test-

resources:                 # ← 决定“发哪些”
  - ../../../base/haivivi-test/test-project

#components:                # ← 插上“公共补丁”
#  - ../../../components/cn-stage/

images:
  - name: cn-haivivi-cn-shanghai.cr.volces.com/libiary/nginx
    newTag: 1.25.5

patches:                   # ← 本组本环境特殊补丁
  - path: patches/syw-test-replica.yaml
  - path: patches/syw-test-env.yaml